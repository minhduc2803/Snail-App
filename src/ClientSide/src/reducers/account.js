const account = (state = {}, action) => {
    switch(action.type){
        case "LOGIN":
            return {

            }
    
        case "LOGOUT":
            return {

            }
        
        case "REGISTER":
            return {
                
            }
        
        default:
            return state;
    }
}

export default account